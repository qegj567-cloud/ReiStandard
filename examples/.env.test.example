# 主动消息 API 测试配置文件
# 复制此文件为 .env.test 并填写实际值
# 使用方法: export $(cat .env.test | xargs) && node test-active-messaging-api.js

# ========== 必需配置 ==========

# 主动消息 API 服务器地址（不是 AI API 地址）
# 本地开发: http://localhost:3000
# 线上环境: https://your-domain.com
# 说明: 测试时不会真正调用 AI API，只测试主动消息 API 的端点连通性
API_BASE_URL=

# Cron Job 认证密钥（用于触发 /send-notifications）
# 必须与服务器端的 CRON_SECRET 环境变量一致
CRON_SECRET=

# 加密主密钥（64位十六进制字符串）
# 生成命令: openssl rand -hex 32
# 必须与服务器端的 ENCRYPTION_KEY 环境变量一致
ENCRYPTION_KEY=

# ========== 可选配置 ==========

# 数据库初始化密钥（仅用于 /init-database 端点，初始化完成后可删除）
# 生成命令: openssl rand -base64 32
# INIT_SECRET=

# 测试用户 ID（默认自动生成: test_user_<timestamp>）
# TEST_USER_ID=test_user_123

# Vercel Protection Bypass 密钥（仅当部署在 Vercel 且启用了 Protection 时需要）
# VERCEL_PROTECTION_BYPASS=

# 测试用 AI API URL（默认: https://api.openai.com/v1/chat/completions）
# TEST_API_URL=https://api.openai.com/v1/chat/completions

# 测试用 AI API 密钥（用于 prompted/auto 类型测试，可以使用占位符）
# TEST_API_KEY=sk-test-placeholder

# 测试用 AI 模型（默认: gpt-5）
# TEST_MODEL=gpt-5
